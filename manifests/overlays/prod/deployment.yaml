apiVersion : apps/v1
kind: Deployment
metadata:
  name: "template-aa"
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: "template-aa"
          command: ["java","-jar","/app.jar"]
          args: ["--spring.config.location=file:/config/application.yml"]
          volumeMounts:
          - mountPath: /mnt/template-aa
            name: "template-aa-volume"
            readOnly: false
            subPath: template-aa
          - mountPath: /config
            name: application-properties
            readOnly: true
                          
      volumes:
      - name: "template-aa-volume"
        persistentVolumeClaim:
          claimName: cepg-aa-std-pvc
      - name: application-properties
        configMap:
          name: "template-aa-configmap"
          items:
            - key: application.yml
              path: application.yml
          defaultMode: 420